# Office 檔案中繼資料清理工具 - 技術文檔

## 架構概述

### 技術堆疊

```
┌─────────────────────────────────────┐
│   User Interface (Windows Forms)   │
│   - TabControl (Settings/Progress) │
│   - Input Validation               │
│   - Event Handlers                 │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│   Core Processing Logic             │
│   - File Scanning                   │
│   - Metadata Cleaning               │
│   - Error Handling                  │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│   Office COM Integration            │
│   - Word.Application                │
│   - Excel.Application               │
│   - PowerPoint.Application          │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│   Report Generation                 │
│   - CSV Export                      │
│   - UTF-8 Encoding                  │
└─────────────────────────────────────┘
```

## 核心模組說明

### 1. UI 模組 (User Interface)

#### 1.1 表單初始化
```powershell
# 主表單
$form = New-Object System.Windows.Forms.Form
$form.Text = "Office 檔案中繼資料清理工具 v2.0"
$form.Size = New-Object System.Drawing.Size(800, 650)
```

#### 1.2 雙標籤設計
- **設置標籤** (`$tabSettings`)：配置處理選項
- **進度標籤** (`$tabProgress`)：顯示執行狀態

#### 1.3 控制項列表

| 控制項類型 | 變數名稱 | 用途 |
|-----------|---------|------|
| TextBox | `$txtFolder` | 目標資料夾路徑 |
| Button | `$btnBrowse` | 開啟資料夾瀏覽器 |
| CheckBox | `$chkWord`, `$chkExcel`, `$chkPowerPoint` | 檔案類型選擇 |
| CheckBox | `$chkTitle`, `$chkSubject`, etc. | 中繼資料項目選擇 |
| CheckBox | `$chkIncludeSubfolders` | 包含子資料夾選項 |
| ProgressBar | `$progressBar` | 進度顯示 |
| TextBox | `$logTextBox` | 日誌輸出 |
| Button | `$btnStart`, `$btnCancel`, `$btnOpenReport` | 控制按鈕 |

### 2. 檔案掃描模組

#### 2.1 掃描邏輯
```powershell
# 建立檔案擴展名列表
$fileExtensions = @()
if ($chkWord.Checked) { $fileExtensions += "*.doc", "*.docx" }
if ($chkExcel.Checked) { $fileExtensions += "*.xls", "*.xlsx" }
if ($chkPowerPoint.Checked) { $fileExtensions += "*.ppt", "*.pptx" }

# 遞迴或非遞迴掃描
foreach ($extension in $fileExtensions) {
    if ($chkIncludeSubfolders.Checked) {
        $files += Get-ChildItem -Path $path -Filter $extension -Recurse -File
    } else {
        $files += Get-ChildItem -Path $path -Filter $extension -File
    }
}
```

#### 2.2 支援的檔案格式

| 應用程式 | 舊格式 | 新格式 |
|---------|--------|--------|
| Word | .doc | .docx |
| Excel | .xls | .xlsx |
| PowerPoint | .ppt | .pptx |

### 3. 中繼資料清理模組

#### 3.1 COM 物件管理

**初始化策略**：單例模式（Singleton Pattern）
- 每種應用程式類型只建立一個 COM 物件實例
- 在所有檔案處理完成後統一釋放

```powershell
function Initialize-OfficeApp {
    param([string]$AppType)
    
    switch ($AppType) {
        "Word" {
            if ($null -eq $script:WordApp) {
                $script:WordApp = New-Object -ComObject Word.Application
                $script:WordApp.Visible = $false
                $script:WordApp.DisplayAlerts = 0
                $script:WordApp.ScreenUpdating = $false
            }
            return $script:WordApp
        }
        # ... Excel 和 PowerPoint 類似
    }
}
```

#### 3.2 中繼資料屬性對照表

| 顯示名稱 | BuiltInDocumentProperties 名稱 | 說明 |
|---------|-------------------------------|------|
| 標題 | Title | 文件標題 |
| 主旨 | Subject | 文件主旨 |
| 作者 | Author | 文件作者 |
| 經理 | Manager | 管理者名稱 |
| 公司 | Company | 公司名稱 |
| 類別 | Category | 文件類別 |
| 關鍵字 | Keywords | 搜尋關鍵字 |
| 備註 | Comments | 文件備註 |

#### 3.3 清理流程

```powershell
function Process-File {
    # 1. 檔案存取檢查
    $fileStream = [System.IO.File]::Open($FilePath, 'Open', 'ReadWrite', 'None')
    $fileStream.Close()
    
    # 2. 保存原始時間戳記
    $originalCreationTime = $fileInfo.CreationTime
    $originalLastWriteTime = $fileInfo.LastWriteTime
    
    # 3. 清理中繼資料
    Clean-XXXMetadata -FilePath $FilePath -MetadataItems $MetadataItems
    
    # 4. 還原時間戳記
    $fileInfo.CreationTime = $originalCreationTime
    $fileInfo.LastWriteTime = $originalLastWriteTime
}
```

#### 3.4 Word 文件處理

```powershell
function Clean-WordMetadata {
    $doc = $app.Documents.Open($FilePath)
    
    try {
        # 清除選定的屬性
        foreach ($item in $MetadataItems) {
            $doc.BuiltInDocumentProperties.Item($item).Value = ""
        }
        
        # 移除隱藏資訊 (RemoveDocumentInformation)
        # 參數 1 = wdRDIAll
        $doc.RemoveDocumentInformation(1)
        
        # 儲存並關閉
        $doc.Save()
    }
    finally {
        $doc.Close()
        [System.Runtime.InteropServices.Marshal]::ReleaseComObject($doc)
    }
}
```

#### 3.5 RemoveDocumentInformation 參數說明

| 值 | 常數名稱 | 說明 |
|----|---------|------|
| 1 | wdRDIAll | 移除所有隱藏資訊 |
| 2 | wdRDIDocumentProperties | 移除文件屬性 |
| 4 | wdRDIComments | 移除註解 |
| 8 | wdRDIRevisions | 移除修訂記錄 |
| 16 | wdRDIVersions | 移除版本資訊 |

### 4. 錯誤處理模組

#### 4.1 異常處理策略

```powershell
try {
    # 處理單個檔案
    Process-File -FilePath $file.FullName
}
catch {
    # 記錄錯誤但不中斷整體流程
    $script:FailedFiles += @{
        Path = $file.FullName
        Error = $_.Exception.Message
    }
}
```

#### 4.2 常見錯誤類型

| 錯誤類型 | 原因 | 處理方式 |
|---------|------|---------|
| IOException | 檔案被鎖定 | 跳過，記錄於報告 |
| UnauthorizedAccessException | 權限不足 | 跳過，記錄於報告 |
| COMException | Office 應用程式錯誤 | 跳過，記錄於報告 |
| FileNotFoundException | 檔案不存在 | 跳過，記錄於報告 |

### 5. 資源清理模組

#### 5.1 COM 物件釋放

```powershell
function Cleanup-OfficeApps {
    try {
        # 1. 退出應用程式
        if ($null -ne $script:WordApp) {
            $script:WordApp.Quit()
            [System.Runtime.InteropServices.Marshal]::ReleaseComObject($script:WordApp)
            $script:WordApp = $null
        }
        
        # 2. 強制垃圾回收
        [System.GC]::Collect()
        [System.GC]::WaitForPendingFinalizers()
    }
    catch {
        Write-Log "Error cleaning up: $($_.Exception.Message)" "Error"
    }
}
```

#### 5.2 資源清理時機

1. **正常完成**：所有檔案處理完成後
2. **使用者取消**：取消操作時立即清理
3. **表單關閉**：視窗關閉事件觸發時

### 6. 報告生成模組

#### 6.1 CSV 報告格式

```csv
檔案路徑,處理狀態,錯誤訊息
"C:\path\file1.docx",成功,
"C:\path\file2.xlsx",失敗,"檔案被鎖定"
```

#### 6.2 報告生成邏輯

```powershell
function Generate-Report {
    $timestamp = Get-Date -Format "yyyyMMdd_HHmmss"
    $reportPath = "Cleanup_Report_$timestamp.csv"
    
    $report = @()
    
    # 成功的檔案
    foreach ($file in $script:ProcessedFiles) {
        $report += [PSCustomObject]@{
            "檔案路徑" = $file
            "處理狀態" = "成功"
            "錯誤訊息" = ""
        }
    }
    
    # 失敗的檔案
    foreach ($file in $script:FailedFiles) {
        $report += [PSCustomObject]@{
            "檔案路徑" = $file.Path
            "處理狀態" = "失敗"
            "錯誤訊息" = $file.Error
        }
    }
    
    # 匯出 CSV (UTF-8 編碼)
    $report | Export-Csv -Path $reportPath -Encoding UTF8 -NoTypeInformation
}
```

## 進階主題

### 效能最佳化

#### 1. COM 物件重用
- 避免為每個檔案建立新的 Office 應用程式實例
- 單例模式確保每種類型只有一個實例

#### 2. 批次處理策略
```powershell
# 按檔案類型分組處理
$wordFiles = $files | Where-Object { $_.Extension -in ".doc", ".docx" }
$excelFiles = $files | Where-Object { $_.Extension -in ".xls", ".xlsx" }
$pptFiles = $files | Where-Object { $_.Extension -in ".ppt", ".pptx" }
```

#### 3. UI 回應性
```powershell
# 定期更新 UI
[System.Windows.Forms.Application]::DoEvents()
```

### 安全性考量

#### 1. 權限模型
- 使用當前使用者權限
- 不提升權限
- 不修改系統設定

#### 2. 檔案鎖定檢查
```powershell
# 獨占模式開啟檔案以檢查鎖定狀態
$fileStream = [System.IO.File]::Open($FilePath, 'Open', 'ReadWrite', 'None')
$fileStream.Close()
```

#### 3. 時間戳記保護
```powershell
# 保存原始時間
$originalCreationTime = $fileInfo.CreationTime
$originalLastWriteTime = $fileInfo.LastWriteTime

# ... 處理檔案 ...

# 還原時間
$fileInfo.CreationTime = $originalCreationTime
$fileInfo.LastWriteTime = $originalLastWriteTime
```

### 除錯與測試

#### 1. 日誌系統
```powershell
function Write-Log {
    param([string]$Message, [string]$Type = "Info")
    
    $timestamp = Get-Date -Format "HH:mm:ss"
    $symbol = switch ($Type) {
        "Success" { "[✓]" }
        "Error" { "[✗]" }
        "Info" { "[i]" }
    }
    
    $logMessage = "$timestamp $symbol $Message"
    $logTextBox.AppendText("$logMessage`r`n")
}
```

#### 2. 測試建議

**單元測試重點**：
1. 檔案掃描邏輯
2. 中繼資料清理函式
3. COM 物件釋放
4. 報告生成

**整合測試場景**：
1. 空資料夾
2. 單一檔案
3. 大量檔案（100+ 個）
4. 混合格式檔案
5. 被鎖定的檔案
6. 損毀的檔案

## API 參考

### 主要函式

#### Initialize-OfficeApp
```powershell
Initialize-OfficeApp -AppType "Word"|"Excel"|"PowerPoint"
```
初始化並返回 Office 應用程式 COM 物件。

#### Process-File
```powershell
Process-File -FilePath "C:\path\file.docx" -MetadataItems @("Title", "Author")
```
處理單個檔案，清除指定的中繼資料項目。

#### Clean-WordMetadata
```powershell
Clean-WordMetadata -FilePath "C:\path\file.docx" -MetadataItems @("Title", "Author")
```
清除 Word 文件的中繼資料。

#### Clean-ExcelMetadata
```powershell
Clean-ExcelMetadata -FilePath "C:\path\file.xlsx" -MetadataItems @("Title", "Author")
```
清除 Excel 工作簿的中繼資料。

#### Clean-PowerPointMetadata
```powershell
Clean-PowerPointMetadata -FilePath "C:\path\file.pptx" -MetadataItems @("Title", "Author")
```
清除 PowerPoint 簡報的中繼資料。

#### Generate-Report
```powershell
Generate-Report
```
生成 CSV 格式的處理報告。

#### Cleanup-OfficeApps
```powershell
Cleanup-OfficeApps
```
清理所有 Office COM 物件並釋放資源。

### 全域變數

| 變數名稱 | 類型 | 用途 |
|---------|------|------|
| `$script:CancelRequested` | Boolean | 使用者取消標誌 |
| `$script:ProcessedFiles` | Array | 成功處理的檔案列表 |
| `$script:FailedFiles` | Array | 失敗的檔案及錯誤訊息 |
| `$script:WordApp` | COM Object | Word 應用程式實例 |
| `$script:ExcelApp` | COM Object | Excel 應用程式實例 |
| `$script:PowerPointApp` | COM Object | PowerPoint 應用程式實例 |
| `$script:ReportPath` | String | 報告檔案路徑 |

## 故障排除

### 常見問題診斷

#### 問題：工具無法啟動
**可能原因**：
- PowerShell 執行原則限制
- .NET Framework 版本不足

**解決方案**：
```powershell
# 檢查 PowerShell 版本
$PSVersionTable.PSVersion

# 臨時允許腳本執行
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

#### 問題：COM 物件建立失敗
**可能原因**：
- Office 未安裝
- Office 版本不相容

**解決方案**：
```powershell
# 檢查 Word 是否可用
try {
    $word = New-Object -ComObject Word.Application
    $word.Quit()
    [System.Runtime.InteropServices.Marshal]::ReleaseComObject($word)
    Write-Host "Word is available"
} catch {
    Write-Host "Word is not available: $($_.Exception.Message)"
}
```

#### 問題：記憶體洩漏
**可能原因**：
- COM 物件未正確釋放

**解決方案**：
- 確保所有 COM 物件使用 `ReleaseComObject`
- 在 Finally 區塊中釋放資源
- 執行垃圾回收

## 延伸功能開發

### 未來版本規劃

#### v2.1 功能清單
- [ ] 支援 Access 資料庫
- [ ] 配置檔保存設定
- [ ] 命令列介面模式
- [ ] 排程任務支援

#### v2.2 功能清單
- [ ] 批次檔案比對
- [ ] 多語言介面
- [ ] 黑暗模式 UI
- [ ] 詳細統計圖表

### 貢獻指南

歡迎開發者貢獻程式碼！請遵循以下規範：

1. **程式碼風格**：使用 PowerShell 最佳實踐
2. **註解**：為複雜邏輯添加清晰註解
3. **錯誤處理**：使用 Try-Catch-Finally
4. **資源清理**：確保 COM 物件正確釋放

## 參考資源

### Office COM API
- [Word Object Model Reference](https://docs.microsoft.com/en-us/office/vba/api/overview/word)
- [Excel Object Model Reference](https://docs.microsoft.com/en-us/office/vba/api/overview/excel)
- [PowerPoint Object Model Reference](https://docs.microsoft.com/en-us/office/vba/api/overview/powerpoint)

### PowerShell
- [PowerShell Documentation](https://docs.microsoft.com/en-us/powershell/)
- [Windows Forms in PowerShell](https://docs.microsoft.com/en-us/dotnet/api/system.windows.forms)

### 中繼資料標準
- [Office Open XML File Formats](https://docs.microsoft.com/en-us/office/open-xml/structure)

---

**文檔版本**：2.0  
**最後更新**：2024 年  
**維護者**：開發團隊
